const pointfree = require('./src/point-free')
const recursion = require('./src/recursion')

/**
 * Two solutions for Euler problem 6
 * https://projecteuler.net/problem=6
 *
 * @author Anton Strand
 */

console.log(pointfree(101))
console.log(recursion(100))
